server:
  port: 9090

eureka:
  client:
    fetchRegistry: true
  instance:
    preferIpAddress: true

feign:
  hystrix:
    enabled: true

employee-search-service:
  ribbon:
    eureka:
      enabled: true